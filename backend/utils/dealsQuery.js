const DEAL_LIST_FIELDS =
  'title partnerName shortDescription category originalPrice discountedPrice discountPercentage isLocked createdAt';

const DEFAULT_SORT = { createdAt: -1 };

module.exports = {
  DEAL_LIST_FIELDS,
  DEFAULT_SORT,
};
